version: 2

jobs:
  build:
    docker:
      - image: secoresearch/apache-varnish:latest
    steps:
    - checkout
    - setup_remote_docker
    - run:
        name: install
        command: apt-get install docker
    -run:
        name: login
        command: docker login -u sri6379 -p Rgs454***
            

workflows:
  version: 2
  build-project:
    jobs:
      - build

       