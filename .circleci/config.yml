version: 2

jobs:
  build:
    docker:
      - image: ubuntu:latest
        auth:
          username: sri6379
          password: Rgs454***
    steps:
    - checkout
    - setup_remote_docker
    - run: |
        apt install python3 -y
        service docker start
        docker run hello-world
workflows:
  version: 2
  build-project:
    jobs:
      - build

       